seed: 42

data:
  source_dir: "/Volumes/Backup/footstep/20250122/4_logmel_all_14"
  splits_dir: "/Volumes/Backup/footstep_m2/data/splits"
  n_folds: 5          # GroupKFold のフォールド数
  test_sessions: []   # held-out test に使うセッションID (例: ["class10_1", "class3_7"])
                      # 空のままなら GroupKFold のみで評価

model:
  name: "SimpleCNN"   # SimpleCNN | ResNet18 | EnvNetV2
  num_classes: 14

training:
  batch_size: 64
  learning_rate: 0.0002
  weight_decay: 0.001
  num_epochs: 200
  optimizer: "Adam"
  early_stopping_patience: 20
  early_stopping_delta: 0.001

experiment:
  name: "baseline_groupkfold"
  output_dir: "/Volumes/Backup/footstep_m2/experiments"
